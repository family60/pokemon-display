import React from "react";

const Type = ({primaryType, secondaryType}) => {
    return(
        <div>
            <p>Type: {primaryType} {secondaryType}</p>
        </div>
    )
}

export default Type;